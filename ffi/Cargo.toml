[package]
name = "rfe-ffi"
version = "0.1.0"
edition = "2024"

[build-dependencies]
cbindgen = "0.29"

[dependencies]
rfe = { version = "0.1.0", path = "../lib" }

[lib]
name = "rfe"
crate-type = ["cdylib", "staticlib"]
doc = false
