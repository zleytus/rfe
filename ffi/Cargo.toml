[package]
name = "rfe-ffi"
version = "0.1.0"
edition = "2021"

[build-dependencies]
cbindgen = "0.26"

[dependencies]
rfe = { path = "../lib" }

[lib]
name = "rfe"
crate-type = ["cdylib", "staticlib"]
